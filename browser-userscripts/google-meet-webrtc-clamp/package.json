{
  "name": "google-meet-webrtc-clamp-userscript",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone userscript that clamps outgoing Google Meet WebRTC video resolution/framerate/bitrate, plus its automated test suite.",
  "scripts": {
    "build": "node scripts/build-userscript.js",
    "test:unit": "node --test src/*.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
