// ==UserScript==
// @name         Google Meet WebRTC Send-Side Clamp
// @namespace    https://github.com/HiromiShikata/termux-app
// @version      1.0.0
// @description  Clamps outgoing Google Meet video resolution/framerate/bitrate to reduce CPU, battery, and network usage.
// @author       HiromiShikata/termux-app
// @match        https://meet.google.com/*
// @run-at       document-start
// @grant        none
// ==/UserScript==
(function(){try{if(window.__termuxMeetWebrtcClampApplied){return;}window.__termuxMeetWebrtcClampApplied=true;var ENABLED=true;var MAX_WIDTH=640;var MAX_HEIGHT=360;var MAX_FRAMERATE=15;var MEET_ORIGIN="https://meet.google.com";if(!ENABLED){return;}if(location.origin!==MEET_ORIGIN){return;}function clampConstraint(value,cap){if(typeof value==='number'){return Math.min(value,cap);}if(value&&typeof value==='object'){var next={};for(var key in value){next[key]=value[key];}if(typeof next.ideal==='number'){next.ideal=Math.min(next.ideal,cap);}if(typeof next.max==='number'){next.max=Math.min(next.max,cap);}else{next.max=cap;}if(typeof next.min==='number'){next.min=Math.min(next.min,cap);}if(typeof next.exact==='number'){next.exact=Math.min(next.exact,cap);}return next;}return {ideal:cap,max:cap};}function clampVideoConstraints(video){if(!video||typeof video!=='object'){return {width:{ideal:MAX_WIDTH,max:MAX_WIDTH},height:{ideal:MAX_HEIGHT,max:MAX_HEIGHT},frameRate:{ideal:MAX_FRAMERATE,max:MAX_FRAMERATE}};}var next={};for(var key in video){next[key]=video[key];}next.width=clampConstraint(video.width,MAX_WIDTH);next.height=clampConstraint(video.height,MAX_HEIGHT);next.frameRate=clampConstraint(video.frameRate,MAX_FRAMERATE);return next;}if(navigator.mediaDevices&&navigator.mediaDevices.getUserMedia){var originalGetUserMedia=navigator.mediaDevices.getUserMedia.bind(navigator.mediaDevices);navigator.mediaDevices.getUserMedia=function(constraints){try{if(constraints&&constraints.video){var next={};for(var key in constraints){next[key]=constraints[key];}next.video=clampVideoConstraints(constraints.video);constraints=next;}}catch(e){}return originalGetUserMedia(constraints);};}function computeScaleDownBy(track){try{var settings=track&&track.getSettings?track.getSettings():null;if(!settings){return 1;}var scale=1;if(typeof settings.width==='number'&&settings.width>MAX_WIDTH){scale=settings.width/MAX_WIDTH;}if(typeof settings.height==='number'&&settings.height/scale>MAX_HEIGHT){scale=settings.height/MAX_HEIGHT;}return scale<1?1:scale;}catch(e){return 1;}}function buildClampedEncodings(existingEncodings,track){var scale=computeScaleDownBy(track);var encodings=existingEncodings&&existingEncodings.length?existingEncodings:[{}];var next=[];for(var i=0;i<encodings.length;i++){var encoding={};for(var key in encodings[i]){encoding[key]=encodings[i][key];}encoding.scaleResolutionDownBy=scale;encoding.maxFramerate=MAX_FRAMERATE;next.push(encoding);}return next;}function applySenderCaps(sender){try{if(!sender||!sender.track||sender.track.kind!=='video'){return;}var parameters=sender.getParameters?sender.getParameters():null;if(!parameters){return;}parameters.encodings=buildClampedEncodings(parameters.encodings,sender.track);sender.setParameters(parameters).catch(function(){});}catch(e){}}function applyAllSenders(pc){try{var senders=pc.getSenders?pc.getSenders():[];for(var i=0;i<senders.length;i++){applySenderCaps(senders[i]);}}catch(e){}}function clampTransceiverInitEncodings(init,track){try{if(!init||typeof init!=='object'){return init;}if(!track||track.kind!=='video'){return init;}var next={};for(var key in init){next[key]=init[key];}next.sendEncodings=buildClampedEncodings(init.sendEncodings,track);return next;}catch(e){return init;}}var NativeRTCPeerConnection=window.RTCPeerConnection||window.webkitRTCPeerConnection;if(NativeRTCPeerConnection){function PatchedRTCPeerConnection(){var pc=new NativeRTCPeerConnection(arguments[0],arguments[1]);try{var originalAddTrack=pc.addTrack.bind(pc);pc.addTrack=function(){var sender=originalAddTrack.apply(pc,arguments);setTimeout(function(){applySenderCaps(sender);},0);return sender;};if(pc.addTransceiver){var originalAddTransceiver=pc.addTransceiver.bind(pc);pc.addTransceiver=function(trackOrKind,init){var clampedInit=clampTransceiverInitEncodings(init,trackOrKind&&trackOrKind.kind?trackOrKind:null);var transceiver=originalAddTransceiver.call(pc,trackOrKind,clampedInit);setTimeout(function(){if(transceiver&&transceiver.sender){applySenderCaps(transceiver.sender);}},0);return transceiver;};}pc.addEventListener('negotiationneeded',function(){setTimeout(function(){applyAllSenders(pc);},0);});}catch(e){}return pc;}PatchedRTCPeerConnection.prototype=NativeRTCPeerConnection.prototype;window.RTCPeerConnection=PatchedRTCPeerConnection;if(window.webkitRTCPeerConnection){window.webkitRTCPeerConnection=PatchedRTCPeerConnection;}}}catch(e){}})();
